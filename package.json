{
  "name": "mithril-interactive-tutorial",
  "version": "0.0.1",
  "description": "Step-by-step lessons on using the Mithril MVC framework",
  "main": "app.js",
  "scripts": {
    "tutorial": "npm run clean-build; npm run make-directories; npm run copy-things; npm run watch-js & npm run watch-stylus & npm run browser-sync",
    "clean-build": "rm -rf build",
    "make-directories": "mkdir build build/styles",
    "copy-things": "cp -Rf src/assets build/ && cp -Rf src/*.html build/",
    "copy-lessons": "cp -Rf src/lesson* build/",
    "browser-sync": "cd ./build && browser-sync start --config=../bs-config.js",
    "watch-stylus": "stylus -w src/styles -o build/styles",
    "watch-js": "watchify src/app.coffee -v -d -t stringify -t coffeeify -o build/bundle.js --list"
  },
  "devDependencies": {
    "browserify": "",
    "coffeeify": "",
    "stylus": "",
    "watchify": "",
    "mithril": "~0.2.0",
    "stringify": "~3.1.0",
    "browser-sync": "~2.7.1"
  },
  "author": "pelonpelon",
  "license": "MIT"
}
